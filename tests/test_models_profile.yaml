# load these models in test_model_parser.py to unit test loading profile yaml models

# exp_decay profile for an amplitude parameter
profile_exp_decay:
    exp_decay:
        A:   [200, True, 10, 1000]
        tau: [2.0, True, 0.5, 10.0]

# linear profile for a position parameter
profile_linear:
    linear:
        m: [-0.5, True, -2, 2]
        b: [0.0, False, -1.0, 1.0]
