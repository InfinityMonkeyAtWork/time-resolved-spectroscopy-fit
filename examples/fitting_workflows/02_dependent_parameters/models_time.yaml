# Use these models to add time-dependence to a parameter in a 2D model
# For available functions see: /src/trspecfit/functions/time.py

sine_wave:
    sinFun:
        A: [-1, True, -5, 5]
        f: [0.25, True, 1E-3, 10]
        phi: [0, True, -0.75, 0.75]
        t0: [0, False]
